import React, { FunctionComponent } from 'react';
import './App.scss';
import { Button } from 'antd';

const App: FunctionComponent = () => (
  <div className="App">
    <Button>Hello</Button>
  </div>
);

export default App;
